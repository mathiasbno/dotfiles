" --- INDENTATION AND PLUGINS --------------------------------------------------

filetype indent plugin on

" --- AUTOCOMMANDS -------------------------------------------------------------

"if has( "autocmd" )
"  autocmd bufwritepost .vimrc source $MYVIMRC
"  autocmd bufreadpost  .vimrc set sts=2 ts=2 sw=2
"endif
